version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: caulong_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: caulong_secret
      POSTGRES_DB: caulong_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata: